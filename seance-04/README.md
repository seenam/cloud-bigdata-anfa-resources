# Dossier captures/

Déposez ici vos quatre captures d'écran obligatoires :

- terraform-plan.png    → sortie de `terraform plan` (création initiale)
- terraform-apply.png   → sortie de `terraform apply` réussi
- console-minio-tf.png  → console MinIO accessible sur http://localhost:9011
- terraform-destroy.png → sortie de `terraform destroy`

Ce fichier README.md peut être supprimé une fois les captures ajoutées.
